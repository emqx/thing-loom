interface:
  display_name: "ESP32 DHT22 + MQTT"
  short_description: "Wire, flash, and view a DHT22 MQTT monitor"
  default_prompt: "Use $esp32-dht22-cloudflare to guide me from wiring an ESP32-C6 and DHT22 through Zero EMQX or custom MQTT broker setup, flashing, and a local or remote dashboard."
